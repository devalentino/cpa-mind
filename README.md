# cpa-mind

Minimal Python console application boilerplate.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
analyze --offer-url https://example.com --trafic-source facebook
```
